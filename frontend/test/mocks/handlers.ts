import { http } from 'msw';

// Define handlers for mocking API requests here.
// Example:
// http.get('/api/user', () => {
//   return HttpResponse.json({ name: 'John Doe' });
// }),

export const handlers = [
  // Add your handlers here
];
